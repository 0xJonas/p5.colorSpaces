{
  "author": "Jonas Rinke <delphi1024@gmail.com>",
  "name": "p5.colorspaces",
  "version": "0.1.0",
  "scripts": {
    "build": "wasm-pack build --target web && rollup -c && cp ./pkg/colorspaces_bg.wasm ./dist/colorspaces_bg.wasm",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "rollup": "^2.50.1"
  }
}
